{% extends 'base.html.twig' %}




{% block main %}

<body>
<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <div class="col-sm-8"><h2>Fila de <b>Atendimento</b></h2></div>
                <div class="col-sm-4">
                    <a href="{{ path('fila') }}" class="btn btn-info add-new"><i class="fa fa-plus"></i>>Adicionar Novo</a>
                </div>
            </div>
        </div>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Codigo Ultimo Usuario</th>
                <th>Ativo</th>
                <th>Açoes</th>
            </tr>
            </thead>
            <tbody>
            <tr>
    {% for post in posts %}
        <tr>


        <td>
            {{ post.codigoFilaAtendimento }}
        </td>
        <td>
            {{ post.nome }}
        </td>
        <td>
            {{ post.codigoUltimoUsuario }}
        </td>
        <td>
            {{ post.ativo }}
        </td>

        <td>
                    <a class="edit" title="Edit" data-toggle="tooltip" href="{{ path('fila') }}"><i class="material-icons">&#xE254;</i></a>
                    <a class="delete" title="Delete" data-toggle="tooltip" href="{{ path('filad') }}"><i class="material-icons">&#xE872;</i></a>
                </td>

            </tr>
        {% endfor %}
            </tbody>
        </table>
    </div>
</div>
</body>














{% endblock %}

